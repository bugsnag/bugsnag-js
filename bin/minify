#!/usr/bin/env sh

cat - | ../../node_modules/.bin/uglifyjs \
  --compress \
  --mangle \
  --source-map "includeSources,content=inline,url=$(basename "$1").map" \
  --output "$1"

  # TODO: Investigate
  # why option was causing build failure
  # --ie \ 
