// @unimodules/core is referenced by the expo-file-system types.
// It is stubbed out in order to avoid depending on a chain of other
// modules just for the type definitions.

declare module '@unimodules/core' {
  export type EventEmitter = any
  export type Subscription = any
}
