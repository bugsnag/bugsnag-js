{
  "name": "bugsnag-js-fixtures-handled-typescript",
  "private": true,
  "scripts": {
    "build": "browserify src/notify_new_error.ts -p [ tsify ] -o dist/notify_new_error.js && browserify src/b.ts -p [ tsify ] -o dist/b.js && browserify src/c.ts -p [ tsify ] -o dist/c.js"
  },
  "dependencies": {
    "browserify": "^16.2.2",
    "tsify": "^4.0.0",
    "typescript": "^4.0.0"
  }
}
